shared class Multimedium(url,format,height,width,type,subtype,caption,copyright) {
    shared String? url;
    shared String? format;
    shared Integer? height;
    shared Integer? width;
    shared String? type;
    shared String? subtype;
    shared String? caption;
    shared String? copyright;
}